# ── Web framework ──
fastapi[standard]==0.115.*
uvicorn[standard]==0.34.*
python-multipart==0.0.20

# ── Database ──
sqlalchemy[asyncio]==2.0.*
asyncpg==0.30.*
alembic==1.15.*

# ── Validation & settings ──
pydantic==2.11.*
pydantic-settings==2.9.*

# ── Auth ──
python-jose[cryptography]==3.4.*
passlib[bcrypt]==1.7.*

# ── HTTP client (Turnstile, Resend, Cloudinary) ──
httpx==0.28.*

# ── Rate limiting ──
slowapi==0.1.*

# ── Images ──
cloudinary==1.*

# ── Dev / test ──
pytest==8.*
pytest-asyncio==0.26.*
ruff==0.11.*
